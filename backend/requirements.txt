fastapi
uvicorn[standard]
python-dotenv
# pysqlcipher3 (disabled, not needed for plain SQLite)
cryptography
click
# google-antigravity-sdk (provided locally, not a pip package)
# mcp-sdk (provided locally, not a pip package)
